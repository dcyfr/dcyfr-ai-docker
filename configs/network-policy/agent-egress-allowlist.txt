# DCYFR agent container outbound allowlist
# One hostname per line. Comments and blank lines are ignored.
#
# Task 1.1.4 requires restricting egress to GitHub + npm package fetch endpoints.

github.com
registry.npmjs.org
objects.githubusercontent.com
